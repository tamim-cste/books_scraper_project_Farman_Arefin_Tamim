# Scrapy project deployment configuration.
# Used by `scrapyd-deploy` to identify which settings module to use.

[settings]
default = books_scraper.settings

[deploy:local]
url = http://localhost:6800/
project = books_scraper
